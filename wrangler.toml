name = "b3nd-org"
pages_build_output_dir = "."
compatibility_date = "2025-06-01"
compatibility_flags = ["nodejs_compat"]

# KV namespace for early-signal email capture.
# Bind via dashboard or `wrangler pages project create` after first deploy:
#   wrangler kv namespace create SIGNUPS
# Then add the resulting id below and re-deploy.
# [[kv_namespaces]]
# binding = "SIGNUPS"
# id = "..."
